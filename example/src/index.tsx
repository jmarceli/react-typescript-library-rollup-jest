import * as React from "react";
import * as ReactDOM from "react-dom";

import { Element } from "react-typescript-library-rollup-jest";

const mountNode = document.getElementById("app");
ReactDOM.render(<Element />, mountNode);
